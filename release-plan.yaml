# CAMARA Release Plan - TEST CASE R4: Missing Mandatory Field
# This tests schema validation for required fields

repository:
  release_track: meta-release
  meta_release: Fall26
  # target_release_tag: MISSING - should cause validation error
  target_release_type: public-release

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: test-api
    target_api_version: 1.0.0
    target_api_status: public
