# CAMARA Release Plan - TEST CASE R8: Invalid Release Tag Format
# This tests schema pattern validation for release tag

repository:
  release_track: meta-release
  meta_release: Fall26
  target_release_tag: v4.1  # INVALID - must use 'r' prefix, not 'v'
  target_release_type: pre-release-alpha

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: test-api
    target_api_version: 0.1.0
    target_api_status: alpha
