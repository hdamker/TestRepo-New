# CAMARA Release Plan - TEST CASE R7: Missing API file for non-draft
# This tests --check-files warning when API file doesn't exist

repository:
  release_track: meta-release
  meta_release: Fall26
  target_release_tag: r4.1
  target_release_type: pre-release-alpha

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: nonexistent-api
    target_api_version: 0.1.0
    target_api_status: alpha  # NOT draft, so file check applies
    # Expected: WARNING - file code/API_definitions/nonexistent-api.yaml not found
