# CAMARA Release Plan - TEST CASE R4: Multiple Errors
# Tests that all schema errors are collected (not just first)

repository:
  release_track: meta-release
  meta_release: Fall26
  # target_release_tag: MISSING - error 1: required field
  target_release_type: public-release

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: test-api
    target_api_version: 1.0.0-rc.1  # error 2: invalid pattern (no extensions)
    target_api_status: beta  # error 3: invalid enum value
