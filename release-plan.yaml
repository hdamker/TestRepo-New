# CAMARA Release Plan - TEST CASE R3: Invalid Enum
# This tests schema validation for invalid enum values

repository:
  release_track: meta-release
  meta_release: Fall26
  target_release_tag: r4.1
  target_release_type: public-release

dependencies:
  commonalities_release: r3.4
  identity_consent_management_release: r3.3

apis:
  - api_name: test-api
    target_api_version: 1.0.0
    target_api_status: beta  # INVALID - should be draft/alpha/rc/public
